<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="css/main.css" />
    <title>Forecast</title>
</head>
<body>
    <div class="content">
        <main>
            <div class="today_date"><h4>Today, <%= data.date %></h4></div>
    
            <div class="city_and_temperature">
                <h5><span><%= data.city%></span>, 
                <span><%= data.temperature %>°C</span></h5>
                <img class="weather_icon--img" src="<%= data.weatherIcon %>" alt="">
            </div>

            <div class="uv_index">
                <h6>The UV index level is <%= data.uv %>. Maximal UV index during the day is 
                <%= data.maxUV %> and is seen at <%= data.maxUVTime %></h6>
            </div>
            
            <div>
                <% if (!locals.yourSafeExposureTime) { %>
                    <form action="/submit-skin-type" method="POST">
                        <input type="text" name="skinType" placeholder="What is your skin type?">
                        <button type="submit">Tell me my safe exposure type</button>
                        <p class="input">Not sure what's your skin type? Click <a href="files/FitzpatrickSkinType.pdf">here</a></p>
                    </form>
                <% } else { %>
                    <div>
                        <% if (locals.exposureTimeData) { %>
                            <p>According to your data, you can be exposed to the sun (without using the sunscreen)
                                for <%=  exposureTimeData %> minutes
                            </p>
                        <% } else { %>
                        <p> Please, provide us a valid skin type number
                        </p>
                        <% } %>
                    </div>
                <% } %>
            </div>
    
            <!-- <div class="motivational_quote"></div> -->
        </main>
    </div>

    <script src="js/client.js" type="text/javascript"></script>
</body>
</html>